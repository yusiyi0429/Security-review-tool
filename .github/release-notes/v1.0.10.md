## v1.0.10 - 2026-07-26

### 修复

- 修复扫描进度页的进度条按默认双向模式绑定只读 `ProgressPercentage`，导致扫描过程中显示 WPF 绑定错误的问题。

### 回归保障

- 所有 `ProgressBar.Value` 绑定均要求显式单向模式。
- 新增 Windows STA 回归测试：加载真实扫描进度页，验证进度条绑定模式为单向并能正确渲染进度。

### 发布说明

- 当前仅发布 Windows 11 x64 安装包和便携版。
- 发布物未使用 Authenticode 签名，请使用随附 `.sha256` 文件核验完整性。
