# Changelog

## 1.0.3 - 2026-07-22

### 新功能

- LLM 配置新增“云上 API”和“内网 / 本机模型”两种端点类型；云端继续强制 HTTPS，内网模型可使用 HTTPS 或受限 HTTP。
- 内网 HTTP 支持私有 DNS 名称，并在建连时将 socket 固定到校验通过的 loopback、RFC 1918 IPv4 或 RFC 4193 IPv6 ULA 地址。

### 修复

- 修复桌面端扫描启动时配置快照无法正确还原，导致任务不能进入实际解析流程的问题。
- 补齐主进程到沙箱 worker 的协议负载、作业处理和解析器注册，使 Windows 扫描链路能够执行真实文件解析。
- 修复二进制输入被文本解析器误判，以及 corpus 清单与验证用例未覆盖相应边界的问题。
- 修复 solution 级构建错误传递 RID、跨平台验证无法准备正确运行资产的问题。
- LLM 连接测试现在会把普通网络失败稳定映射为请求错误，而不是让异常逸出到界面。

### 安全

- 内网 HTTP 拒绝公网、link-local 和云元数据地址；禁止自动重定向、隐式代理和跨 origin 请求。
- 云上 API 仍要求系统信任的 HTTPS 证书；界面提示云端数据边界以及内网 HTTP 未加密风险。

### 验证

- Debug 与 Release 全解决方案构建均为 0 警告、0 错误。
- macOS 可执行的 LLM exact-origin 合约测试 20/20 通过；Windows 发布流水线继续运行 Unit、Contract、Integration 和 ParserCorpus 验证。

### 发布说明

- 当前发布物未使用 Authenticode 证书签名；请在运行前核对随附的 SHA-256 校验文件。
- macOS 尚不受支持：当前桌面层和安全隔离依赖 WPF、AppContainer、Job Object 与 DPAPI。

## 1.0.2 - 2026-07-22

### 新功能

- 全面升级桌面交互界面，新增专业安全工作台侧栏、统一卡片、表单、状态反馈、扫描进度与结果视图。
- 新增应用品牌图标，并统一安装程序、开始菜单、桌面快捷方式和窗口图标。

### 修复

- 修复主窗口缺失画刷资源和转换器作用域错误导致安装后无法启动的问题。
- 修复 WPF 独立窗口装载时根级静态资源解析失败，并新增运行时 XAML 与资源闭包回归测试。
- 启动失败时持久化脱敏诊断事件，并区分主窗口与启动失败对话框，提升安装验证可靠性。
- 修复发布脚本在 `win-x64` 锁定还原后未沿用 RID 进行构建和测试的问题。

### 验证

- 单元测试 765/765、契约测试 294/294 全部通过。
- UI 启动回归测试 4/4 通过，桌面项目以 0 警告、0 错误完成 Release 构建。
- 已在 Windows 本机真实启动新版 EXE，确认主窗口、导航、应用图标和核心扫描页面正常显示。

### 发布说明

- 当前发布物未使用 Authenticode 证书签名；请在运行前核对随附的 SHA-256 校验文件。

## 1.0.1 - 2026-07-22

### 新功能

- 新增 Windows 当前用户安装包，支持开始菜单快捷方式、可选桌面快捷方式、安装后启动和原位升级。
- 新增安装包 SHA-256 校验文件，并支持使用 Authenticode 证书签名安装包。

### 修复

- 修复主窗口 XAML 布局错误导致 Release EXE 启动后不显示界面的问题。
- 修复部分 Windows 环境缺少 `windir` 变量时 WPF 字体缓存初始化崩溃的问题。
- 启动失败时写入诊断日志、显示安全化错误信息并退出，避免残留无界面进程。
- 修复扫描取消时终态结果丢失的竞态，以及重现问题、预览和命令状态更新中的边界行为。
- 修复 LLM 端点校验、同源连接、鉴权、TLS 错误、重试截止时间、响应大小限制及提示注入净化问题。
- 修复规则包签名、清单校验、策略优先级和 Excel 内联字符串读取问题。
- 修复 Windows 发布脚本中的锁定还原、ZIP 创建、SBOM、签名参数及包内容校验问题。

### 验证

- 单元测试 763/763、契约测试 294/294 全部通过。
- 正式发布包校验 10/10 通过，未发现已知易受攻击或弃用的 NuGet 包。
- 已验证静默安装、缺少 `windir` 时启动、原位重复安装、卸载，以及卸载后保留用户数据。

### 文档

- 更新安装、升级、卸载、便携版使用和发布验证说明。

### 发布说明

- 当前发布物未使用 Authenticode 证书签名，按试点版本分发；请在运行前核对随附的 SHA-256 校验文件。
