<UserControl x:Class="SecurityReview.Desktop.Views.RuleManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:SecurityReview.Desktop.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:RuleManagementViewModel}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Active rule pack info -->
        <Border Grid.Row="0" Padding="12" CornerRadius="6"
                Background="#E8F0FE" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock FontSize="16" FontWeight="Bold" Text="当前规则包"/>
                <TextBlock Margin="0,4,0,0" FontSize="13">
                    <Run Text="ID: "/><Run Text="{Binding ActiveRulePackId}"/>
                </TextBlock>
                <TextBlock FontSize="13">
                    <Run Text="版本: "/><Run Text="{Binding ActiveVersion}"/>
                </TextBlock>
                <TextBlock FontSize="13">
                    <Run Text="哈希: "/><Run Text="{Binding ActiveHash}"/>
                </TextBlock>
                <TextBlock FontSize="13" Foreground="Red"
                           Text="{Binding Warnings}" TextWrapping="Wrap"
                           Margin="0,4,0,0"/>
            </StackPanel>
        </Border>

        <!-- Actions -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="导入规则包" Command="{Binding ImportCommand}"
                    Padding="8,4" Margin="0,0,6,0"/>
            <Button Content="刷新" Command="{Binding RefreshCommand}"
                    Padding="8,4"/>
        </StackPanel>

        <!-- Import status -->
        <TextBlock Grid.Row="2" Text="{Binding LastImportStatus}"
                   FontSize="12" Foreground="Gray" Margin="0,4,0,0"/>

        <!-- History list -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="0,30,0,0">
            <ItemsControl ItemsSource="{Binding History}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="LightGray" BorderThickness="1"
                                Padding="8,4" Margin="0,0,0,3" CornerRadius="3">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontWeight="SemiBold"
                                           Text="{Binding RulePackId}"/>
                                <TextBlock Text=" v" Foreground="Gray"/>
                                <TextBlock Text="{Binding Version}"
                                           Foreground="Gray"/>
                                <TextBlock Text=" (" Foreground="Gray"/>
                                <TextBlock Text="{Binding StatusDisplay}"
                                           Foreground="Gray"/>
                                <TextBlock Text=")" Foreground="Gray"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
