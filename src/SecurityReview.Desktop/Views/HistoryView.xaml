<UserControl x:Class="SecurityReview.Desktop.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:SecurityReview.Desktop.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:HistoryViewModel}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
            <Button Content="刷新" Command="{Binding RefreshCommand}"
                    Padding="8,4" Margin="0,0,6,0"/>
            <Button Content="重新扫描" Command="{Binding RescanCommand}"
                    Padding="8,4" Margin="0,0,6,0"/>
            <Button Content="删除" Command="{Binding DeleteCommand}"
                    Padding="8,4"/>
        </StackPanel>

        <!-- Scan list -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Scans}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="LightGray" BorderThickness="1"
                                Padding="10,8" Margin="0,0,0,4" CornerRadius="4">
                            <StackPanel>
                                <TextBlock FontWeight="SemiBold" FontSize="14">
                                    <Run Text="{Binding CreatedAtDisplay}"/>
                                    <Run Text=" · "/>
                                    <Run Text="{Binding StatusDisplay}"/>
                                </TextBlock>
                                <TextBlock Text="{Binding RiskSummary}"
                                           FontSize="12" Foreground="Gray"
                                           Margin="0,4,0,0"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
